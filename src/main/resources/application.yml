#默认使用配置
spring:
  profiles:
    active: dev

#公共配置与profiles选择无关
mybatis:
  typeAliasesPackage: com.yuan.blog.domain
  mapperLocations: classpath:mapper/*.xml
  # mybatis 配置
  configLocation: classpath:mybatis-config.xml

pagehelper:
    helperDialect: mysql
    reasonable: true
    supportMethodsArguments: true
    params: count=countSql

---

#开发配置
spring:
  profiles: dev

  datasource:
    url: jdbc:mysql://47.90.97.197:3306/blog?serverTimezone=GMT%2B8&useSSL=false&characterEncoding=UTF-8
    username: root
    # password: Ali@2526176
    driver-class-name: com.mysql.jdbc.Driver
    type: com.alibaba.druid.pool.DruidDataSource

